// they are presentational component
// props.username= "krits" cannot be done since its only readonly prop
// props are readonly or immutable

export default function UpdatingProps(props) {
    return (
        <div>
            <h1>Lets try to update props....</h1>
        </div>
    )
}